(defcfg
  process-unmapped-keys yes
)

(defsrc
  caps h j k l           
  v
  lctl
)

(deflayer default
  lctl _ _ _ _ 
  @v       
  caps
)
(deflayer arrows
  _ left down up rght          
  _ 
  _
  
)
(defalias
  v (tap-hold-press 200 200 v (layer-toggle arrows))
)
